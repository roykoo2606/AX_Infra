# WORKFLOW — 작업 절차와 템플릿 (v0.3)

> CONSTITUTION.md의 원칙을 실제 작업으로 옮기는 구체 절차. 모든 에이전트 공통.

## 1. 기본 작업 흐름

```
지시 수신 → TASKS.md 확인·등록 → 이해 확인 → 실행 → 산출물 저장 → TASKS.md 갱신 + 로그 기록 → 결과 보고
```

1. **지시 수신**: 대화, 또는 `inbox/`의 파일
2. **TASKS.md 확인·등록**: 진행 중인 작업과 충돌하는지 확인 후 새 작업을 등록 (아래 §2-1)
3. **이해 확인**: 모호하면 실행 전에 1개 질문으로 확인. 명확하면 바로 실행
4. **실행**: 승인 규칙(CONSTITUTION §3) 준수. 장시간 작업은 중간보고 1회

## 2-1. TASKS.md — 공유 작업 보드 (전원 필수)

- **이 파일이 모든 에이전트의 공유 상황판이다.** Hermes가 모바일 지시로 직접 일할 때도 반드시 여기 등록 후 시작한다
- 등록: ID(`T-YYYYMMDD-nn`), 작업, 담당, 시작일을 '진행 중' 표에 추가
- 갱신: 상태 변화 시 즉시. 완료 시 '완료' 표로 이동(최근 10건만 유지, 상세는 logs/)
- 다른 에이전트의 진행 중 작업을 발견하면 중복 착수하지 않고, 지원이 필요해 보이면 비고에 남긴다
5. **산출물 저장**: 프로젝트 작업이면 `projects/<프로젝트>/output/`, 아니면 지시에 명시된 위치
6. **TASKS.md 갱신 + 로그 기록**: 보드 상태 갱신, 로그는 아래 §4 포맷
7. **결과 보고**: 산출물 위치 + 핵심 결과 3줄 이내

## 2. inbox/ — 원격 지시 처리

- 파일명: `YYYY-MM-DD_<제목>.md`
- 에이전트는 작업 시작 시 `inbox/`에서 `status: new`인 파일을 확인한다
- 처리 후 같은 파일에 결과를 병기하고 status를 갱신한다 (파일 이동 없음 — 단순하게)

```markdown
---
status: new | in-progress | done
---
# 지시: <제목>
<지시 내용>

## 처리 결과 (에이전트 작성)
- 완료일: YYYY-MM-DD
- 결과: <요약>
- 산출물: <경로>
```

## 3. 프로젝트 절차

**시작** — `projects/<kebab-name>/` 생성 후 두 파일 작성:

```markdown
# GOAL.md
**하나의 목표**: <한 문장>

## 완료 조건
- [ ] <검증 가능한 조건 1>
- [ ] <조건 2>
```

```markdown
# RULES.md
<CONSTITUTION·WORKFLOW와 다른 점만. 없으면 "전체 규약을 그대로 따른다" 한 줄>
```

**진행** — 산출물은 `output/`, 중간 메모는 `notes/` (필요 시에만 생성)
**종료** — GOAL.md 완료 조건 전부 체크 → 폴더를 `projects/_archive/`로 이동 → 로그 기록

## 4. logs/ — 기록 포맷

파일: `logs/YYYY-MM-DD_<주제>.md` (같은 날 같은 주제는 이어서 작성)

```markdown
# YYYY-MM-DD <주제>
- 지시: <무엇을 요청받았나 (출처: 대화/inbox)>
- 결과: <무엇을 만들었나/바꿨나>
- 산출물: <경로 또는 커밋>
- 결정: <새로 확정된 규칙·결정 (없으면 생략)>
- 다음: <남은 일 (없으면 생략)>
```

## 5. 에이전트 역할 분담

| 역할 | 담당 | 환경 | 비고 |
|---|---|---|---|
| 오케스트레이터·메인 작업 | Claude Code CLI | cmux/tmux | 지시 해석, 작업 분배, 개발·산출물, 최종 검수·보고 |
| 단순작업 워커 | Codex CLI, Antigravity CLI | tmux | 반복작업, 이미지 생성, 파싱, 웹서치·크롤링 |
| 감시 자동화 | Antigravity CLI | tmux | Google Drive 감시, 작업 진행 감시 |
| 모바일 창구 | Hermes(Discord) | 상주 | 지시 수신 → inbox/·TASKS.md 기록 → 직접 수행 가능, 완료 알림 |
| 협업·진행 공유 | Cowork | 앱 | 팀 협업, 진행상황 공유 |
| 뷰어 | Obsidian | 앱 | 시각화·자료 열람 전용 |
| (향후) 로컬 LLM | 요약·분류 등 경량 작업 | - | 도입 시 이 표 갱신 |

- 어떤 에이전트든 진입점(CLAUDE.md/AGENTS.md)의 읽기 순서를 따른다
- 에이전트 간 정보 전달은 **파일로만** 한다 (이 저장소가 공유 메모리)

## 5-1. 에이전트 기본 모델 (CONSTITUTION §0 라우팅 적용)

각 에이전트는 아래 **기본 모델**로 시작하고, §0 규칙(막히면 상향·남으면 하향, `effort` 미세조정)에 따라 조정한다.
공급자가 다르면 티어 이름도 다르다 — 외부 도구의 정확한 모델 ID는 도구 설정과 이 표를 **동기화**한다.

| 에이전트 | 공급자 | 기본 모델 | 상향(어려울 때) | 근거 |
|---|---|---|---|---|
| Claude Code(CLI) | Anthropic | **Opus 4.8** | Fable 5 | 오케스트레이션·해석·최종검수는 §0의 '복잡·중요'. 실제 코딩·조사 하위작업은 **Sonnet 5**, 기계적 작업은 **Haiku 4.5** 하위 에이전트로 위임 |
| codexx (Codex) | OpenAI | **GPT-5.5 (medium)** | GPT-5.5 (high) | 워커: 파싱·반복·크롤링 등 단순작업. 복잡 코드작업만 상향 |
| agyx (Antigravity) | Google | **Gemini 3.5 Flash (high)** | Gemini 3.5 Pro | 워커·감시 자동화: 속도·비용 우선 |
| Hermes(Discord) | Anthropic | **Haiku 4.5** | Sonnet 5 | 지시 수신·기록 중심 경량 창구 |
| (향후) 로컬 LLM | 로컬 | 경량 모델 | - | 요약·분류 전용 |

> **원칙 요약**: 오케스트레이터(Claude Code)만 상위 티어를 기본으로 쓰고, 나머지 워커·창구는 각 공급자의 **빠르고 저렴한 티어**를 기본으로 한다. 난도가 높은 개별 작업에서만 상향한다.
> 외부 도구(codexx/agyx) 모델 ID는 도구 버전에 따라 바뀌므로, 새 버전 도입 시 이 표를 갱신한다(§5와 동일 원칙).

## 6. 산출물 규칙

- 회사 제출용: docx/xlsx/pptx로 생성, `output/`에 저장, 파일명에 날짜 (`2026-07-05_주간보고.docx`)
- 내부 참고용: 마크다운
- 같은 내용을 두 형식으로 만들지 않는다. 제출용이 필요할 때만 변환

## 7. Handoff — 에이전트 간 작업 인계 양식

작업을 다른 에이전트에게 넘길 때 `logs/handoff_<task_id>.md`로 남긴다:

```markdown
# Handoff: <task_id>
## 완료한 것 / 읽은 파일 / 수정한 파일 / 내린 결정
## 리스크·블로커 / 필요한 사용자 승인 / 권장 다음 단계
```
