# 2026-07-07 실행 환경 최종 확정 — cmux 네이티브 세션 복원
- 지시: cmux-layout.sh 실패 원인 분석, 자동화 계속 시도 (출처: 대화)
- 결과: cmux 공식 README 확인 — (1) 종료 시 세션 저장, 재실행 시 레이아웃·cwd·스크롤백 자동 복원, (2) `cmux hooks setup`으로 Claude Code/Codex 세션 자동 재개 지원. 분할 스크립트 자체가 불필요했음. 실패 원인: surface 참조를 추측(surface:N)으로 작성
- 최종 구조: tmux(ax)=hermes/watcher 무인 서비스만(launchd 자동 기동) · cmux=claudex/agyx/codexx 직접 실행 + 네이티브 복원. cmux-layout.sh 폐기(안내문으로 대체), view.sh는 서비스 로그 열람용으로 유지
- 산출물: scripts/start.sh(서비스 전용 개정), scripts/cmux-layout.sh(폐기 안내)
- Roy 1회 설정: cmux hooks setup && cmux hooks setup codex → 패인 구성(⌘D/⌘⇧D) → 이후 자동
- 참고: Antigravity(agyx)는 cmux 공식 resume 지원 목록에 없음 — 재시작 시 그 패인만 agyx 재입력 필요할 수 있음
