# 01. 워크플로우 구축 계획 (v0.1)

> 작성일: 2026-07-05 | 근거: Roy의 6개 구성요소 정의
> 상태: 보고 → 승인 후 Phase 1부터 착수

## 1. 요구 분석 — Roy가 원하는 것

1. **역할이 나뉜 멀티에이전트 팀**: Hermes(모바일 창구) / Claude Code(메인·오케스트레이터) / Codex·Antigravity(단순작업 워커) / Antigravity(감시 자동화) / Cowork(협업·공유) / Obsidian(뷰어)
2. **전원 같은 작업 상황을 안다**: Hermes가 모바일 지시로 직접 일해도, Claude Code와 다른 에이전트가 그 내용을 알고 지원할 수 있어야 함
3. **한 번의 스크립트로 기동**: 재부팅·기기 이전 시 스크립트 하나로 전체 시스템 자동 실행
4. **대시보드**: 지금 누가 무슨 일을 어디까지 했는지 한눈에 모니터링

## 2. 시스템 구성도

```
[모바일] Discord ──> Hermes ──┐ 지시 기록
                              ▼
                    ┌─────────────────────────┐
                    │  AX_Infra 저장소 (공유 뇌) │
                    │  inbox/  TASKS.md  logs/ │
                    └─────────────────────────┘
                       ▲          ▲         ▲
        (읽기/쓰기)     │          │         │
  Claude Code CLI ─────┘   Codex CLI   Antigravity CLI
  (오케스트레이터,          (단순작업     (단순작업 워커 +
   cmux/tmux)              워커, tmux)   GDrive 감시, tmux)

  [보기 전용] Obsidian(파일 뷰어) · 대시보드(STATUS) · Cowork(협업 공유)
```

**핵심 설계 = 공유 작업 보드 `TASKS.md`**
누가 일하든 반드시 TASKS.md에 등록·갱신한다. 이 파일이 "전원이 같은 상황을 아는" 유일한 장치다.
- Hermes가 모바일 지시를 받으면: `inbox/`에 지시 기록 + `TASKS.md`에 작업 등록 → 그 다음 직접 수행
- Claude Code는 세션 시작 시(그리고 작업 전) TASKS.md를 읽어 진행 중인 일을 파악하고, 필요하면 워커에게 분배
- 에이전트 간 통신은 파일로만 (WORKFLOW §5 원칙 유지)

## 3. 단계별 구축 계획

| Phase | 내용 | 산출물 | 완료 조건 |
|---|---|---|---|
| **1. 공유 보드** | TASKS.md 도입 + WORKFLOW 규칙 갱신 | TASKS.md, WORKFLOW v0.2 | 두 에이전트가 같은 보드로 작업 인계 1회 성공 |
| **2. 표준 세션** | **cmux 메인 + 내부 tmux**: tmux 세션(창: claude/codex/antigravity/watcher)은 백그라운드 상주, cmux가 각 패인에서 attach하는 시각 레이어 | scripts/start.sh | 명령 1개로 전체 세션 기동, cmux에서 전체 조망 |
| **3. 자동 기동** | 로그인 시 자동 실행(launchd) + bootstrap.sh 갱신 | scripts/install-autostart.sh | 재부팅 후 자동 복구 확인 |
| **4. Hermes 연동** | Discord 봇 설치, inbox/TASKS 기록 규칙 주입 | Hermes 설정 + 지침 | 모바일 지시 → inbox 기록 → 처리 → Discord 알림 1회 성공 |
| **5. GDrive 감시** | Antigravity 감시 에이전트 (폴더 변화 → TASKS 등록/알림) | watcher 지침·스크립트 | 파일 변경 감지 → Discord 알림 1회 성공 |
| **6. 대시보드** | v1: STATUS.md 자동 생성(Obsidian·모바일에서 확인) + Discord #status 채널. v2(보류): 로컬 HTML 대시보드 | scripts/status.sh | 어디서든 30초 안에 전체 상황 파악 가능 |

원칙: **한 번에 한 Phase씩.** 각 Phase는 완료 조건 검증 후 다음으로.

## 4. 대시보드 전략 (심플 우선)

- **v1 (Phase 6)**: `STATUS.md` 한 파일 — TASKS.md 요약 + 최근 로그 + 에이전트 가동 상태를 스크립트가 주기 생성. Obsidian에서 열고, Hermes가 같은 내용을 Discord #status로 발송 → 모바일 대시보드 해결
- **v2 (보류)**: 로컬 웹 대시보드. v1이 부족할 때만

## 5. 결정 필요 사항

- [x] 실행 환경 (2026-07-05 확정): **cmux 메인 + cmux 안에서 tmux 실행**. tmux 세션은 백그라운드에 상주해 자동 기동·재부팅 생존을 담당하고, cmux는 그 위의 시각·조작 레이어 (GUI 없이도 시스템은 동작)
- [ ] Hermes 설치 상태 확인 필요 (미설치라면 Phase 4에서 가장 쉬운 설치 경로로)
- [ ] Cowork 역할 범위: 진행상황 공유를 Cowork 세션으로 할지, Discord #status로 충분한지
- [ ] GDrive 감시 대상 폴더와 감지할 이벤트(새 파일/수정/특정 형식)

## 6. 보류 항목 연계

- [GitHub-연동] 완료 후 이 저장소가 실제로 기기 간 동기화됨 — Phase 3(이전·복구)의 전제이므로 Phase 3 전까지는 연동 권장
