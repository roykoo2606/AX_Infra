# AX_Infra 기획 최초안 (v0.1)

> 작성일: 2026-07-03 | 상태: 초안 — 질문/답변을 통해 지속 보완
> 이 문서는 AX_Infra 기획의 출발점이다. 모든 후속 지침은 이 문서에서 파생된다.

---

## 1. 비전

**"어디서든(모바일·맥·웹) 하나의 데이터를 보고, 하나의 목표를 향해 일하는 AI 에이전트 인프라"**

- 모든 도구와 에이전트가 **같은 데이터(단일 소스)** 를 본다
- **전체 규약(Constitution)** 아래 프로젝트별 하위 지침이 파생된다
- 재부팅해도 스스로 살아나고, 다른 맥/노트북에 그대로 이식된다
- 구성은 극단적으로 **심플**하게 — 복잡함이 현재의 가장 큰 문제

## 2. 현재 보유 구성요소

| 구성요소 | 역할(현재) | 비고 |
|---|---|---|
| Obsidian + 터미널 | 지식베이스 + 작업 공간 | Claude Code, Codex, Antigravity CLI를 vault에서 실행 |
| cmux | 멀티에이전트 터미널 | 병렬 에이전트 실행, 패인 간 출력 공유, 내장 브라우저 (macOS 전용) |
| Hermes (Discord) | 원격 지시/알림 채널 | 모바일에서 에이전트에게 지시 가능 |
| Antigravity IDE | 개발 IDE | |
| Claude Code 데스크톱/모바일 | 에이전트 인터페이스 | |
| 로컬 LLM (향후) | 프라이버시/비용 절감용 | 추후 도입 |

## 3. 설계 원칙 (초안)

1. **Single Source of Truth** — 데이터는 한 곳(Obsidian vault + Git)에만 존재. 모든 도구는 그것을 바라본다
2. **규약의 계층화** — 전체 규약 1개 → 프로젝트별 지침 파생. 중복 작성 금지
3. **Plain Text First** — 마크다운 파일 기반. 어떤 도구·LLM도 읽을 수 있어야 함
4. **이식성** — 새 머신에서 스크립트 하나로 전체 환경 복원 (`bootstrap.sh`)
5. **자가 기동** — 재부팅 후 자동 복구 (launchd/자동 시작)
6. **도구는 교체 가능, 데이터는 영구** — cmux든 Hermes든 도구가 바뀌어도 데이터·규약은 그대로

## 4. 아키텍처 초안 (4계층)

```
┌─ 인터페이스 계층 ──────────────────────────────┐
│  Claude 모바일/데스크톱 · Discord(Hermes) · 웹    │
└──────────────────┬───────────────────────────┘
┌─ 오케스트레이션 계층 ─┴───────────────────────┐
│  cmux (멀티에이전트) · Claude Code · Codex ·    │
│  Antigravity CLI/IDE · (향후) 로컬 LLM          │
└──────────────────┬───────────────────────────┘
┌─ 데이터 계층 ──────┴──────────────────────────┐
│  Obsidian Vault = Git 저장소 (단일 소스)        │
│  규약 + 프로젝트 지침 + 노트 + 작업 로그          │
└──────────────────┬───────────────────────────┘
┌─ 실행 계층 ────────┴──────────────────────────┐
│  상시가동 맥 or 클라우드 VM (24/7 호스트)        │
│  + 각 개인 기기 (동기화된 복제본)                │
└──────────────────────────────────────────────┘
```

## 5. 규약 구조 (초안)

```
AX_Infra/  (= Git 저장소 루트, Obsidian vault)
├── CONSTITUTION.md      ← 전체 규약 (모든 에이전트·프로젝트에 적용)
├── CLAUDE.md / AGENTS.md ← 각 에이전트가 자동으로 읽는 진입점 (규약을 참조)
├── bootstrap.sh          ← 새 머신 이식용 셋업 스크립트
├── projects/
│   ├── <프로젝트A>/
│   │   ├── GOAL.md       ← 이 프로젝트의 목표 (하나의 목표)
│   │   └── RULES.md      ← 규약에서 파생된 하위 지침
│   └── <프로젝트B>/ ...
├── inbox/                ← 모바일/원격에서 던진 지시가 쌓이는 곳
└── logs/                 ← 에이전트 작업 기록
```

- **CONSTITUTION.md**: 말투, 언어, 파일 규칙, 안전 규칙, 데이터 위치 등 불변 원칙
- **projects/*/GOAL.md + RULES.md**: 목표 중심. 프로젝트가 끝나면 아카이브
- 모든 에이전트(Claude Code, Codex, Antigravity)는 진입 시 CONSTITUTION → 해당 프로젝트 RULES 순으로 읽는다

## 6. 동작 시나리오 (목표 상태)

1. **모바일에서**: Discord(Hermes) 또는 Claude 모바일로 지시 → 상시가동 호스트의 에이전트가 수행 → 결과가 vault에 기록 → 어디서든 확인
2. **맥에서**: Obsidian에서 cmux 터미널 열고 멀티에이전트 병렬 작업. 오케스트레이터(Claude Code)가 하위 에이전트 지휘
3. **재부팅 후**: launchd가 동기화·Hermes·필수 데몬 자동 기동
4. **새 머신**: `git clone` + `bootstrap.sh` 실행이면 끝

## 7. 확정된 결정사항 (2026-07-03)

- [x] **데이터 동기화**: Git + GitHub private 저장소. 모바일은 Obsidian Git 플러그인 또는 호스트 경유 접근
- [x] **24/7 호스트**: 맥 상시가동 (잠자기 방지 + launchd 자동 기동 설정)
- [x] **원격 지시 채널**: Hermes(Discord) 중심 — 호스트 에이전트에 직접 지시·알림 수신
- [x] **첫 프로젝트**: AX_Infra 구축 자체 (`projects/ax-infra-setup/`) — 규약→지침 구조를 실전 검증
- [x] **Obsidian의 역할**: 단순 뷰어 + 자료 정리 도구. 별도 볼트 지침 불필요
- [x] **승인 경계**: 기본은 자율. 승인 필요 = 중요 시스템 파일 삭제, 외부 전송, 비용 발생. 일반 파일 삭제는 휴지통 이동으로 대체. 세부 경계는 사용하며 조정
- [x] **언어**: 문서·규약은 한국어, 코드·커밋 메시지는 영어 (에이전트 효율 기준)
- [x] **상시가동 맥**: 아직 없음 — 조만간 구축 예정. 현재 맥은 대부분 켜져 있는 상태로 운용

## 8. 열린 질문 (다음 라운드)

- [ ] 에이전트 역할 분담: 오케스트레이터는 누구? (Claude Code 추천) 하위 에이전트 배치는?
- [ ] Hermes(Discord) 연동 구성 방법
- [ ] 로컬 LLM 도입 시점과 용도
- [ ] GitHub 저장소 이름·구조, 민감정보(.env, 토큰) 관리 방식

## 9. 참고

- cmux: AI 에이전트 전용 macOS 터미널 — https://goddaehee.tistory.com/557 / https://github.com/manaflow-ai/cmux
