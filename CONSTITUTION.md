# CONSTITUTION — AX_Infra 전체 규약 (v0.1)

> 모든 에이전트(Claude Code, Codex, Antigravity, 향후 로컬 LLM)에 적용되는 최상위 규약.
> 프로젝트별 지침(projects/*/RULES.md)은 이 규약을 위반할 수 없다.

## 1. 데이터 원칙

- 이 저장소(AX_Infra)가 **유일한 데이터 소스**다. 모든 산출물·기록은 여기에 남긴다
- 모든 문서는 마크다운(plain text). 어떤 도구·LLM도 읽을 수 있어야 한다
- Obsidian은 뷰어·자료 정리 용도일 뿐이다. Obsidian 전용 기능에 의존하지 않는다

## 2. 언어

- 문서·규약·사용자 소통: **한국어**
- 코드·커밋 메시지·파일명(코드 관련): **영어**

## 3. 승인 규칙

기본은 자율 수행. 아래 3가지만 **사전 승인 필수**:

1. 중요 시스템 파일·설정의 삭제 또는 변경
2. 외부로의 데이터 전송 (이메일 발송, 외부 API 업로드, 공개 저장소 push 등)
3. 비용이 발생하는 작업 (유료 API, 클라우드 자원 생성 등)

추가 규칙:

- 일반 파일 삭제는 강제 삭제(rm) 대신 **휴지통 이동**을 기본으로 한다
- 승인 경계는 운영하며 조정한다. 조정 시 이 문서를 갱신한다

## 4. 규약 계층

- 에이전트는 진입 시 `CONSTITUTION.md` → 해당 프로젝트의 `GOAL.md`, `RULES.md` 순으로 읽는다
- 하위 지침은 상위 규약과 중복 작성하지 않는다. 다른 점만 기술한다
- 프로젝트가 끝나면 `projects/_archive/`로 이동한다

## 5. 심플함 원칙

- 새 도구·규칙·자동화를 추가할 때, 제거할 수 있는 기존 것이 없는지 먼저 검토한다
- 지침은 짧게. 한 문서가 한 화면을 넘으면 분리보다 삭제를 먼저 고려한다

## 6. 기록

- 의미 있는 작업 완료 시 `logs/YYYY-MM-DD_<주제>.md`에 한 줄 이상 기록한다
- 원격(inbox/)에서 받은 지시는 처리 후 결과를 같은 파일에 병기한다
